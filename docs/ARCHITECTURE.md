# Architecture

The browser owns intake, deterministic scoring, local persistence, rendering, and export. There is no provider call, background worker, or hidden decision authority in the production core.
